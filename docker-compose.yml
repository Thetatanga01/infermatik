# After pulling: rebuild so the image matches the new code (and lockfile) —
#   docker compose build --no-cache && docker compose up -d
services:
  web:
    build: .
    ports:
      - "9090:80"
    restart: unless-stopped
